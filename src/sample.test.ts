import { describe, it, expect } from "vitest";

// Sample placeholder test to keep CI green until real tests are added

describe("sanity", () => {
  it("works", () => {
    expect(1 + 1).toBe(2);
  });
});
